#include <openssl/evp.h>
#include <string.h>
#include "crypto.h"

void encrypt_note(const char *plaintext, const char *password, char *ciphertext) {
    // Mã hóa ghi chú bằng AES-256
}

void decrypt_note(const char *ciphertext, const char *password, char *plaintext) {
    // Giải mã ghi chú
}
